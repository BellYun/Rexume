FROM node:20-alpine

WORKDIR /app

# curl 설치 (healthcheck용)
RUN apk add --no-cache curl

# 의존성 파일 복사
COPY package*.json ./

# 의존성 설치
RUN npm ci

# 소스 코드 복사
COPY . .

# Next.js 개발 서버 포트
EXPOSE 3000

# 개발 서버 실행
CMD ["npm", "run", "dev"]

